# QuotaPing 安装说明

1. 双击 **安装 QuotaPing.command**。
2. 如果 macOS 拦截脚本，右键点击它，选择“打开”。
3. 仍然不能打开的话，打开“设置” -“隐私与安全”-“安全”，找到“QuotaPing”选择允许。
4. 脚本会安装到当前用户的 `~/Applications/QuotaPing.app`，并自动启动。

## 成功启动后 QuotaPing 仅显示在菜单栏，不会显示 Dock 图标或普通窗口。
